/* eslint-disable no-unused-vars */

// Arity
// number of arguments

// the arity is binary
const sum = (a, b) => a + b

// the arity is unary
const inc = (a) => a + 1

// this arity is nullary
const zero = () => 0
